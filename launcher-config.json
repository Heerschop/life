{
  "scripts": {
    "serve": [
      [
        "lite-server",
        "build --watch"
      ]
    ],
    "build": [
      "rimraf dist",
      [
        "cpx 'src/assets/**/*' dist/assets",
        "cpx 'src/*.{html,css}'      dist --verbose $*",
        "tsc $*"
      ]
    ]
  }
}
